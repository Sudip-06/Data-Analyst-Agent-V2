# railway.toml
[build]
dockerfilePath = "Dockerfile"   # be explicit that we use the Dockerfile

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
